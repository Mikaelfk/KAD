image: "mikaelfk/bachelor_ci:1.0"

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

cache:
  paths:
    - .cache/pip
    - venv/

stages:
  - Test
  - Linting

pytest:
  stage: Test
  script:
  - cd backend/
  - pip3 install virtualenv
  - virtualenv venv
  - source venv/bin/activate
  - pip3 install pytest 
  - printf "[JHOVE]\nJhoveInstallPath=/usr/bin" > config
  - pytest tests

reacttest:
  image:
    name: node:17.6.0
  cache:
  directories:
    - node_modules
  stage: Test
  script:
  - npm install
  - npm test


sonarqube-check:
  image: 
    name: sonarsource/sonar-scanner-cli:latest
    entrypoint: [""]
  variables:
    SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar"  # Defines the location of the analysis task cache
    GIT_DEPTH: "0"  # Tells git to fetch all the branches of the project, required by the analysis task
  cache:
    key: "${CI_JOB_NAME}"
    paths:
      - .sonar/cache
  script: 
    - sonar-scanner
  allow_failure: true
  only:
    - main # or the name of your main branch
  stage: Linting
